# Executive dashboard preview

`index.html` is a lightweight standalone executive preview of the Protect → Grow → Expand framework.

It is intentionally not the analytical source of truth. Final analytical definitions and metrics are documented in `docs/` and `results/`.

The repository does not include raw customer/product files or a production Power BI `.pbix` file.
