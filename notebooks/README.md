# Analysis notebooks

The exploratory work was developed in DuckDB-based notebooks during the assessment. The public repository keeps the reusable SQL and validation scripts as the auditable implementation layer rather than publishing every exploratory cell.

The final executive metrics are captured in `results/` and explained in `docs/`.
